{
  "name": "shop-app",
  "version": "1.0.0",
  "description": "customer can shop",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"cd code-app && npm run build",
    "install-client":"cd code-app && npm install",
    "heroku-postbuild":"npm run install-client && npm run build",
    "start": "node app.js"
  },
  "author": "Ashwin Rao K",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.6",
    "nodemon": "^2.0.2"
  }
}
